// ------------------------------------------------------------------
// 方向クラス
// ------------------------------------------------------------------

class Destination {
  constructor(top, left) {
    this.top = top;
    this.left = left;
  }
}
