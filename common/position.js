// ------------------------------------------------------------------
// 位置クラス
// ------------------------------------------------------------------

class Position {
  // コンストラクタ
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}
